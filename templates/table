import * as app from "../app"

export interface {{ name }} {
  // type of table
}

const table = new app.Table<{{ name }}>({
  name: "{{ name }}",
  setup: (table) => {
    // setup table columns => http://knexjs.org/#Schema-Building
  },
})

export default table