import * as app from "../app"

const command: app.Handler = {
  name: "{{ name }}",
  async run(message) {
    // todo: code here
    await message.reply("{{ name }} command is not yet implemented.")
  }
}

module.exports = command